// Find the last element of the following arrays.
// arr1 = [3,7,34,90,12]
// arr2 = [true, "green", "where",12,56]

arr2 = [true, "green", "where",12,56]
let arr1=[3,7,34,90,12];
console.log(arr1.length);
console.log(arr1[4]);

let arr2 = [true, "green", "where",12,56];
console.log(arr2.length);
console.log(arr2[4]);

// Write a JS program that will join the following array elements into a string
// myPets = ["Cow", "Python", "Snake", "Dog"];
let myPets = ["Cow", "Bird", "Snake", "Dog"];
   let myPets2=myPets.toString();
   console.log(myPets2);

//    Write a JS script to sort the following array items--it sorts the values in the list in ascending order
// var arr3 = [ -5, 9, 5, 3, 2, -3, 6, 8, 4, 1 ];
   let arr3 = [ -5, 9, 5, 3, 2, -3, 6, 8, 4, 1 ];
   let arrr=arr3.sort();
   console.log(arrr);

//    Write a program to remove duplicates from the following array.
// Console the array without duplicates, and console another array that only contains the duplicates
// var arr = ["apple", "mango", "apple",
            // "orange", "mango", "mango"];
var arr = ["apple", "mango", "apple",
            "orange", "mango", "mango"];
   var arr = ["apple", "mango", "apple",
            "orange", "mango", "mango"];
            function remove(a)
            
            // console.log(remove(arr));
            // Write a JS script to search for the following word in the array.
            // 4
            // If the word is present, console it else console "the search word was not found"
            // let arr5 = ["the", "way", "x", 4, 23];

let arr5 = ["the", "way", "x", 4, 23];
   var arr = ["apple", "mango", "apple",
   "orange", "mango", "mango"];
 let is =  ["apple", "mango", "apple",
           "orange", "mango", "mango"]
    let uniqueIs= [...new Set(is)];
    console.log(uniqueIs);
            let (arr5 = ["the", "way", "x", 4]);
          if (arr5=="way"){
              console.log("way");
            }
          else{
                 console.log("the search word was not found");
            }
            let word = "sevink";
            let word2=word.split('').join().sort('');
            console.log(word2);
            // // Find the last element of the following arrays.
            // // arr1 = [3,7,34,90,12]
            // // arr2 = [true, "green", "where",12,56]

            // arr6  = [3,7,34,90,12];
            // arr7 = [true,"green","where",12,56];
            // console.log(arr1.length);
            // // console.log(arr1)
