// implicit-internally
let a ="10";
let b =10;
// a *=1;
console.log(a+b);
// or console.log(+a+b);

// explicit-we use javascript methods and its externally
console.log({b:String(b)});
console.log({a:Array(a)});
